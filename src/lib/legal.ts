// One place to change the facts that appear on every public legal page. Meta's App
// Dashboard requires these URLs to be live and consistent before it will issue a
// permanent WhatsApp access token, and a reviewer reads them — so the contact address
// must be one that is actually monitored.
//
// ⚠️ CHANGE THIS to the address you want the public (and Meta) to write to. It appears
// on /privacy-policy, /terms and /data-deletion.
export const LEGAL_CONTACT_EMAIL = "aashiq4cinemas@gmail.com";

export const LEGAL_SERVICE_NAME = "AM 360";

/** Day-month-year, matching how every other date in the app renders (src/lib/date.ts). */
export const LEGAL_UPDATED = "24 July 2026";
